// Write a C program to find the remainder of two numbers.
#include<stdio.h>
int main()
{
    int a = 50;
    int b = 7;
    int remainder;

    remainder = 50 % 7;
    printf("Finally the reminder is = %d\n", remainder);
    return 0;
}